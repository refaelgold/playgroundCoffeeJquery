// Generated by CoffeeScript 1.6.3
(function() {
  var currentElement, footer, header, mainHeader, menuTop, newP, slideUpBox, testBox, triggerForBox;

  mainHeader = $('h1#mainTitle');

  menuTop = $('.nav');

  header = $('.header');

  footer = $('.footer');

  testBox = $('#box001');

  slideUpBox = $('#box002');

  triggerForBox = $('#trigger');

  currentElement = $(this);

  mainHeader.css({
    "color": "red",
    "font-size": "50px"
  });

  if (!mainHeader.hasClass('extraTitleClass')) {
    mainHeader.addClass('extraTitleClass');
  }

  if (!menuTop.hasClass('show' && !header.hasClass('show' && !footer.hasClass('show')))) {
    menuTop.addClass('hide');
    header.addClass('hide');
    footer.addClass('hide');
  }

  testBox.animate({
    "opacity": 0,
    "left": 500,
    "top": 100
  }, 5000, function() {
    return console.log("END OF ANIMATE");
  });

  testBox.attr({
    'style': ''
  });

  triggerForBox.on("click", function() {
    return slideUpBox.slideToggle();
  });

  $('p').remove();

  $('h1').empty().text("this is a new header");

  $('h1').wrap('<hgroup></hgroup>');

  $('h1').unwrap('<hgroup></hgroup>');

  $('.childen').wrapAll('<div/>');

  newP = $('<p></p>', {
    'text': "hello worldljds",
    'class': 'newP'
  });

  $("h1").append(newP);

}).call(this);

/*
//@ sourceMappingURL=main.map
*/
